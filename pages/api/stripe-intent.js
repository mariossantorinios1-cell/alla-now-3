export default function handler(req, res) {
  // Placeholder: implement server-side Stripe integration here.
  res.status(200).json({ ok: true, message: 'Replace with Stripe server-side code and secret key.' })
}
